npx sequelize migration:create --name=nome_tabela   -> Para criar um arquivo de configuração de migration database/migrations

npx sequelize db:migrate    -> Para fazer a migração para a base de dados
